package tools

import (
	"context"
	"io"
	"net/http"
)

func HandleWanakuInfo(ctx context.Context, args map[string]interface{}) (ToolResponse, error) {
	name, _ :=getString(args, "name")
	if name == ""

---
*deepseek-reasoner (deepseek)*
}